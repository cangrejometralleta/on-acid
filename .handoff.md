# Checkpoint

**Intent** — Make the build fetch its own tooling, open the same doors on Windows, publish the branch, and Measure the version floor instead of guessing it.

**Done** — `build.sh` now Carries `install_packages`, which creates `.venv` and installs ruff, mypy and build, so no step Skips any more. The format gate Became a lint gate (`ruff check`): the aligned species tables and the order of `__all__` are Deliberate, and a formatter would Reflow both. `pyproject.toml` Declares `line-length = 100` and `select = ["E", "F", "B"]`. Added `build.cmd`, `run.cmd` and the one helper `run-bash.cmd`, plus `.gitattributes` for the line endings. Fixed the one real finding: `test_a_species_is_frozen` Asserted a blind `Exception` and now Names `dataclasses.FrozenInstanceError`.

**Validation** — The suite Passes on 3.9, 3.10, 3.11, 3.12, 3.13 and 3.14 under `uv`, and on 3.8 as well. The 0.2.0 wheel Installs into a 3.9 environment, where all 20 exported names Resolve and the `on-acid` command Runs. `./build.sh` exits 0 with every step Running for real: ruff clean, mypy clean, 63 tests passing, and `on_acid-0.1.0.tar.gz` plus `on_acid-0.1.0-py3-none-any.whl` built. The shims are ASCII with CRLF, the two of them four lines each, and `run-bash.cmd` Searches Git for Windows, then the PATH with the System32 launcher discarded, then WSL with the path translated, then Refuses with an address.

**Open** — The `.cmd` files are Unverified: they were read from Linux and never Run on Windows. The floor in `requires-python` Sits at 3.9 although 3.8 Passes too, which is a choice about what to support, not a limit the code Imposes.

**State** — Branch `main` Carries its commits and Tracks `origin`. The package Sits at 0.2.0.

**Next** — Await the user's Instruction. Running the shims on a Windows machine and a plotted distribution diagram are the open candidates.
