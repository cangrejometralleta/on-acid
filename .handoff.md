# Checkpoint

**Intent** — Turn the single module into an installable library with a test gate, a license and a first commit.

**Done** — Added `pyproject.toml` (flat layout, distribution `on-acid`, import `dissociation`, no dependencies, `requires-python >=3.9`, `setuptools>=77` for the PEP 639 license field). Added `LICENSE` (MIT). Added `build.sh` and `run.sh` under the Scripts rule, and `.gitignore` for `.agents/`, `.codex/` and build residue. Dove wrote `test_dissociation.py`: 63 tests on `unittest`, entering through the public functions only. Deleted `disosiacion.py`. Lifted the demo out of `if __name__ == "__main__"` into `demo()`, split across `_demo_species`, `_demo_buffer` and `_demo_titration`, exported it and declared `[project.scripts] on-acid = "dissociation:demo"`. Extended the README with Doors, Install, Tests and License.

**Validation** — `./build.sh` exits 0 with the 63 tests passing; its test gate was proven by a canary failure, which exits 1 and names the line that works. The package installs into a clean venv; from outside the checkout all 20 names in `__all__` import and resolve, and the installed `on-acid` command reproduces the documented demo (2.875, 4.745, 8.722, 11.959). `License-Expression: MIT` lands in the metadata.

**Open** — ruff, mypy and `build` are absent on this machine, so those three steps of `build.sh` are Unverified beyond their skip path. `requires-python >=3.9` is reasoned from the syntax, never run on a 3.9 interpreter. The repository has no remote push yet.

**State** — Branch `main` Carries the first commit. `.agents/` and `.codex/` are ignored rather than dirty. Git had no configured identity; the commit Set it on this repository only.

**Next** — Await the user's Instruction. A push to `origin`, a plotted distribution diagram and the absent linters are the open candidates.
